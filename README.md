# joshfung.dev - WIP Personal Site

### Build and Deploy via SWA CLI

- Build using: `npm run build` -> Outputs to `dist` folder
- Deploy using: `npx swa deploy --env production`
  - Can test deployment using: `npx swa start dist`
